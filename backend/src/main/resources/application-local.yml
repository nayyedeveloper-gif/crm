spring:
  jpa:
    show-sql: true
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration
  flyway:
    # Local DB may have manually applied migrations during demos
    validate-on-migrate: false

# Shop Google Sign-In (Web client ID — public; must match NEXT_PUBLIC_GOOGLE_CLIENT_ID)
app:
  shop:
    google-client-id: 843876433428-kluefoiahsfhchnvapj8p7uich9rjl5i.apps.googleusercontent.com

logging:
  level:
    com.salecrm: DEBUG
