spring:
  jpa:
    show-sql: false
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration
  flyway:
    validate-on-migrate: true

logging:
  level:
    root: INFO
    com.salecrm: INFO
