# Include from http {} context (e.g. /etc/nginx/conf.d/crm-image-cache.conf)
proxy_cache_path /var/cache/nginx/crm_images
    levels=1:2
    keys_zone=crm_images:64m
    max_size=2g
    inactive=14d
    use_temp_path=off;
